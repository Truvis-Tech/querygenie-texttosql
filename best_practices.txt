{
  "best_practices": [
    "Always select only the columns you need instead of using SELECT *, because SELECT * scans all columns and increases both cost and query time.",
    "Apply WHERE filters as early as possible so BigQuery can prune rows before scanning unnecessary data, reducing costs.",
    "Use LIMIT or TABLESAMPLE when testing or sampling, since they restrict data scanned and speed up development iterations.",
    "Avoid ORDER BY without a LIMIT clause, because sorting the entire dataset is expensive and unnecessary if you only need a subset.",
    "Prefer GROUP BY instead of DISTINCT when aggregating, as GROUP BY is more efficient and provides the same results.",
    "Use approximate aggregate functions like APPROX_COUNT_DISTINCT or APPROX_QUANTILES for large datasets to reduce scan cost while keeping statistical accuracy.",
    "Filter directly on partition columns (e.g., a DATE or TIMESTAMP column) so BigQuery can prune partitions and minimize scanned data.",
    "Match filters to the partition column\u2019s datatype from schema metadata to avoid implicit casts that prevent partition pruning.",
    "Avoid wrapping partition columns in functions (e.g., DATE(timestamp_col)) in WHERE clauses, since this disables pruning.",
    "After applying partition filters, verify in the EXPLAIN plan that partitions are actually pruned to confirm efficiency.",
    "Use table decorators (tablename@YYYYMMDD) for point-in-time queries to limit scans to a single partition.",
    "Do not over-partition tables into many small partitions (<1MB), because this increases metadata overhead and query planning costs.",
    "Leverage pseudo-columns (_PARTITIONTIME or _PARTITIONDATE) for ingestion-time partitioned tables to filter efficiently.",
    "Choose clustering columns by selectivity, placing the most selective column first, to maximize pruning and reduce data scanned.",
    "Ensure WHERE clause filters align with the defined clustering column order so clustering can reduce scanned data effectively.",
    "Combine partition pruning with clustering filters to maximize efficiency and reduce the scan footprint.",
    "Do not use more than four clustering columns, because clustering beyond this limit provides diminishing returns.",
    "Cluster on columns that are frequently filtered or used as join keys to gain the most benefit from pruning.",
    "Avoid clustering on very high-cardinality string columns, as this may not provide meaningful cost reduction.",
    "Use broadcast joins for small dimension tables (<10 GB), so they can be replicated across workers and reduce shuffle cost.",
    "Place the most selective filters before JOINs so fewer rows are processed in the join stage.",
    "Use explicit INNER JOINs instead of WHERE-based joins, since INNER JOINs are more efficient and easier to optimize.",
    "Order JOINs from smallest to largest intermediate result sets, which reduces shuffle costs and memory usage.",
    "Use the USING clause when joining on identically named columns, since it improves clarity and avoids redundant conditions.",
    "Prefer SEMI JOIN over EXISTS subqueries when checking for membership, as SEMI JOINs are more efficient in BigQuery.",
    "Use ANTI JOIN instead of NOT EXISTS when excluding matches, since ANTI JOINs are faster and more scalable.",
    "Filter both tables before joining whenever possible, so that unnecessary rows do not reach the join stage.",
    "Pre-filter large tables in CTEs before joining them, which reduces intermediate result sizes and join costs.",
    "Avoid redundant CTE expansions; only materialize CTEs when reused multiple times to prevent repeated scans.",
    "Avoid cross joins unless absolutely necessary, because they produce Cartesian products and can explode data size.",
    "Use partitioned joins when both tables are partitioned on the same join key to minimize data scanned.",
    "Apply explicit join hints like /*+ BROADCAST_JOIN */ to control execution strategies for cost optimization.",
    "Use hash joins for large-to-large table joins, because they scale better than nested loop approaches.",
    "Denormalize small dimension data into fact tables to reduce the need for frequent joins.",
    "Convert correlated subqueries into JOINs when possible, since JOINs are more efficient and easier to optimize.",
    "Use EXISTS instead of IN with subqueries, because EXISTS avoids scanning the full subquery result set.",
    "Replace scalar subqueries in SELECT with LEFT JOINs or window functions to avoid repeated execution.",
    "Use WITH clauses (CTEs) instead of deeply nested subqueries, as they improve readability and reusability.",
    "Limit CTE nesting to three or four levels to avoid unnecessary complexity and repeated scans.",
    "Give CTEs descriptive names and chain them logically to make queries more maintainable and understandable.",
    "Reuse CTEs in queries instead of repeating expressions to reduce redundant scans.",
    "Use QUALIFY to filter window function results directly, avoiding extra subqueries or outer filters.",
    "Partition window functions on clustered columns to reduce the data BigQuery must process in each partition.",
    "Order window functions by clustered or indexed columns for better performance.",
    "Combine multiple window functions that share the same PARTITION BY clause to avoid multiple passes over the data.",
    "Use unbounded preceding/following sparingly, since these require scanning the entire partition and can be expensive.",
    "Prefer RANGE clauses over ROWS in window functions when appropriate, as RANGE leverages ordering for efficiency.",
    "Use LAG/LEAD instead of self-joins for time-series analysis, because they are more efficient and readable.",
    "Prefer FIRST_VALUE and LAST_VALUE over complex aggregations when retrieving boundary values, since they are optimized.",
    "Implement running totals using SUM() OVER (ORDER BY \u2026), which avoids repeated subqueries or joins.",
    "Use statistical window functions (PERCENT_RANK, NTILE, CUME_DIST, CORR, etc.) where needed to reduce manual calculations.",
    "Always align join keys with consistent datatypes from schema metadata, and use SAFE_CAST where mismatched, to avoid full scans caused by implicit casting.",
    "Prefer built-in SQL functions over UDFs, because UDFs prevent BigQuery from pushing down optimizations and increase runtime costs.",
    "Use STRING type only for text and BYTES only for binary, since misusing types increases storage and scan costs.",
    "Choose DATE, DATETIME, and TIMESTAMP appropriately, aligning with actual usage, to prevent unnecessary conversions.",
    "Use ARRAY and STRUCT types for nested or repeated data, reducing the need for costly joins.",
    "Use GEOGRAPHY for spatial data, as it enables optimized spatial functions instead of string approximations.",
    "Prefer NUMERIC over FLOAT64 for precise decimal values to avoid rounding errors in financial or critical calculations.",
    "Denormalize small dimensions into fact tables to reduce repeated join costs and query complexity.",
    "Use nested or repeated fields when appropriate to model hierarchical data without requiring join expansion.",
    "Prefer wide, denormalized tables for read-heavy analytics workloads instead of highly normalized transactional schemas.",
    "Use partitioned tables instead of sharded suffix tables (_YYYYMMDD), since partitioned tables scale better and are easier to manage.",
    "Design schemas around query access patterns rather than strict transactional normalization to reduce query costs.",
    "Use views to present normalized interfaces on denormalized tables, balancing usability with performance.",
    "Group by low-cardinality columns first to improve efficiency of aggregation stages.",
    "Use GROUP BY column positions for cleaner syntax and easier query maintenance.",
    "Use ROLLUP or CUBE for multi-dimensional aggregations instead of writing multiple queries.",
    "Pre-aggregate data in CTEs before repeating the same aggregations in later stages to avoid recomputation.",
    "Use HAVING only for filtering aggregated results, not raw rows, to maintain query efficiency.",
    "Create materialized views for frequent aggregations to avoid recalculating results every time.",
    "Use COUNT(*) when counting all rows, as it is optimized compared to COUNT(column).",
    "Use SUM(CASE WHEN \u2026) for conditional aggregations instead of running multiple separate aggregation queries.",
    "Prefer COUNTIF and SUMIF for conditional counts and sums, as they are concise and optimized.",
    "Use ARRAY_AGG and STRING_AGG to collect rows into arrays or strings instead of manual concatenation queries.",
    "Filter arrays at the UNNEST stage to prevent unnecessary row explosions.",
    "Use ARRAY_AGG with ORDER BY to ensure deterministic array results when order matters.",
    "Avoid repeating expressions by assigning them to CTEs or subqueries so they are computed once.",
    "Use LIKE for simple pattern matches instead of REGEXP_CONTAINS, since LIKE is cheaper and faster.",
    "Use REGEXP_REPLACE instead of multiple REPLACE calls, as it consolidates string operations efficiently.",
    "Use LOWER or UPPER for case-insensitive comparisons to improve consistency in string matching.",
    "Prefer FORMAT or CONCAT for string construction instead of verbose expressions for readability and efficiency.",
    "Use DATE_TRUNC, DATE_ADD, DATE_SUB, and EXTRACT for date arithmetic to leverage built-in optimizations.",
    "Use CURRENT_DATE() instead of CURRENT_TIMESTAMP() when only the date is needed, reducing unnecessary precision.",
    "Generate date ranges using GENERATE_DATE_ARRAY instead of manual unions to simplify and optimize queries.",
    "Partition window functions by fixed time intervals (day, month, year) to align with query patterns and reduce cost.",
    "Use time-based clustering for temporal query patterns to improve pruning and reduce scanned data.",
    "Set query priorities (BATCH vs INTERACTIVE) depending on workload type to optimize slot allocation and costs.",
    "Use reservations or flex slots for predictable workloads to avoid on-demand slot variability.",
    "Monitor slot usage through INFORMATION_SCHEMA to identify query bottlenecks and high slot consumers.",
    "Always tag queries with labels for cost attribution and tracking by team or workload.",
    "Set query timeouts to prevent runaway queries from consuming excessive resources.",
    "Use DRY RUN to estimate scan costs before running expensive queries.",
    "Prioritize optimizations that reduce bytes scanned when dry run shows high scan volume.",
    "Use EXPLAIN to analyze execution plans and verify partition pruning, join strategies, and stage efficiency.",
    "Assign expiration times for temporary or staging tables to avoid paying for unused storage.",
    "Implement lifecycle policies for data cleanup to control storage costs over time.",
    "Balance clustering benefits with increased storage costs, as clustering reorganizes data physically.",
    "Regularly monitor storage costs and remove unused or redundant data.",
    "Leverage INFORMATION_SCHEMA to track storage, query usage, and costs at table and project level.",
    "Set alerts for queries that exceed defined scan or cost thresholds to catch inefficiencies early.",
    "Enable result caching and use query parameterization to increase cache reuse and reduce repeated costs.",
    "Avoid ORDER BY without LIMIT in reporting queries, since sorting entire datasets is costly.",
    "Use BI Engine for frequently accessed small datasets to reduce query latency and costs.",
    "Use SAFE_CAST and SAFE_DIVIDE to avoid runtime errors and prevent query failures.",
    "Use COALESCE or IFNULL for handling NULL values cleanly and preventing unnecessary errors.",
    "Use ASSERT statements in SQL to enforce data quality checks during pipeline execution.",
    "Track query performance and shuffle stages with Query Insights to identify and address bottlenecks.",
    "Use authorized views to enforce row-level security without duplicating tables.",
    "Apply column-level security with policy tags to protect sensitive fields without restricting access to entire tables.",
    "Encrypt sensitive data both at rest and in transit to comply with security requirements.",
    "Enable audit logging for all data access to maintain transparency and compliance.",
    "Always use service accounts with least privilege to enforce principle of minimal access.",
    "Use VPC-SC to restrict network-based data exfiltration risks for sensitive datasets.",
    "Apply data retention policies and customer-managed encryption keys when regulatory compliance requires them.",
    "Track data lineage to maintain compliance reporting and understand upstream/downstream dependencies.",
    "Apply anonymization or masking techniques for sensitive fields to protect PII during query processing.",
    "Monitor data access patterns continuously to detect anomalies and enforce compliance policies."
  ]
}
